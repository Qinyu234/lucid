---
description: 优化阶段专用算法规则（待补充）
alwaysApply: false
---

# 优化算法规则（预留）

> **状态：留白** — 未来在此补充固定任务上的最优算法选型、替换流程与评测标准。

## 预期内容（占位，暂不执行）

- 未来 **merger** 阶段的算法选型、替换流程与评测标准  
- 用户 job 的 `src/shared` 仅限库 / 校验 / 日志（§3.1），算法实现不得塞入 shared  
- 相对基线的性能评测与回退条件  

编译器 **不再** 维护 `pipeline/algorithm/` 或 `io/output/algorithm/`；在本文档充实之前，优化阶段**不要**臆造算法库或批量替换实现。
