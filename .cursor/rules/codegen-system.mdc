---
description: Code generator 双层说明（编译器 vs 用户生成契约）
alwaysApply: true
---

# Code Generator System

## 两层（不要混用规则）

| 阶段 / 范围 | 规则文件 | 用途 |
|-------------|----------|------|
| **生成项目代码** | `codegen-user-contract` §1–§4 | 单函数、命名、import、骨架 |
| **优化项目代码** | `codegen-user-contract` §5 + `codegen-algorithms`（留白） | 只锁文件夹架构；性能优先；算法待补 |
| **本仓库编译器** `src/` | `codegen-src` + `verify_src_contract.py` | pipeline 维护 |
| **pipeline 产出路径** | `codegen-output` | `io/output/workplace/` |

## Node（pipeline 元数据）

`semantic`, `function_name`, `io.in`, `io.out`, `role`, `topology`

## Emission 要点

- Composite → `render_init` 模板（非 LLM 填 `__init__.py`）
- Leaf → LLM + `verify_code`；函数名 = `function_name`
- 禁止生成泛用 `def run()` 作为 leaf 主函数

## Do not

- 用 `codegen-src` 的 import 表去生成用户项目（用户项目用 `src.shared` 表）。
- 跳过 `verify_code` / 骨架检查就注册 leaf。
- 交付前：`python -m src.verify_workplace_contract <job_root> <job_id>`。
