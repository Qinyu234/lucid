---
description: 生成物路径约定（io/output/workplace 等）；模块契约见 codegen-user-contract
globs: io/output/**/*.py
alwaysApply: false
---

# 生成物路径（本仓库 pipeline 产出）

**模块形状、import、项目骨架**以 `codegen-user-contract` 为准（`src/` + `src/shared/` + 包 `__init__` 接口）。

本仓库将成品写入 `io/output/workplace/<job_id>/`，布局应对齐用户契约：

```
io/output/workplace/<job_id>/
  src/                 # 与 user-contract 一致
  io/
  requirement/
  run.py
```

## Node 元数据（pipeline 单源）

每个 node：`semantic`, `function_name`, `io.in`, `io.out`, `role`, `topology`。

## Emission（编译器行为，不改变用户契约）

- Composite → `render_init` 模板生成 `__init__.py`（函数名 = 文件夹名）。
- Leaf → 函数名 = `function_name` = 文件 stem；生成后 AST 校验。
- SEQ：`ctx = child(ctx)`；ROUTER：`ctx["meta"]["case"]`。

## 校验工具

- 树与文件布局：`python tools/assess_workplace.py <job_id>`
- 编译器本体：`python tools/verify_src_contract.py`（仅 `src/`，非生成 job）
