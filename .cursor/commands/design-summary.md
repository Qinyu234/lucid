# 设计阅读与总结

请阅读本仓库中与 **{{模块/路径}}** 相关的代码与规则（`codegen-system`、`codegen-src`、`codegen-output`），并输出：

1. **当前设计**：数据流、关键模块、与 node/topology 的关系  
2. **约束**：已落地的 rules 与 `verify_src_contract` 要求  
3. **缺口**：与「本地 7B / 小模型」或「严格静态 compile」目标的差距  
4. **可选方向**：2～3 条改进路径（各 1 句利弊）

**不要改代码**。我会根据你的总结继续对话调整方向。
