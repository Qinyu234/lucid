# 目录 / 命名纠偏

请调整目录职责（最小迁移，更新所有 import 与配置引用）：

- **问题**：{{如「config 不应在 output」「X 应迁到 io/output」}}  
- **目标结构**：{{目标路径说明}}  

## 要求
- 不破坏现有 job 的 workplace 路径约定  
- 更新 `config/`、`io/` 下引用；grep 确保无残留旧路径  
- 简短列出迁移映射表  

不要改无关业务逻辑。
