# 性能优化（仅守骨架）

对 **`{{路径或 job_id}}`** 做**优化阶段**改动（不是搭骨架）：

## 必须保持（codegen-user-contract §4–§5）
- 根目录：`src/`、`io/`、`requirement/`、`run.py`
- 既有包路径、`__init__.py` 接口、**文件名 = 函数名** 不变
- 不合并/拆分模块文件，不新增平行根目录

## 优化目标
1. **时间**：{{如延迟、吞吐}}
2. **空间**：{{如峰值内存}}
3. （可选）可观测性：经 `src.shared` 打日志/指标

## 允许
- 只改各模块**顶层函数体内**及嵌套 `def` 的实现
- 必要时在 `src/shared/` 增加性能工具（仍仅标准库底层）

## 禁止
- 臆造 `codegen-algorithms` 中尚未定义的算法库或批量替换策略
- 破坏 import/单函数契约以外的**目录架构**

## 交付
- 列出改动文件与性能理由（预期收益 + 风险）
- 说明如何 benchmark：`{{命令或数据}}`
