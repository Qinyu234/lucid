# 设计抉择（咨询）

我在 **{{主题，如 topology / memory / growth 停止条件}}** 上不确定，请结合现有实现给建议：

**背景**：{{一两句现状或倾向，如「认为 topology+模板 init 更优」}}

**待定项**（请逐项对比并给出推荐默认值）：
1. {{选项 A vs B}}  
2. {{节点数量 / 嵌套深度 / 是否高级版}}  
3. {{与 7B 模型能力、生成成本、可维护性的权衡}}

输出格式：**推荐方案** → **理由** → **若选另一方案的风险** → **最小可落地第一步（不改无关代码）**。

先讨论，**不要直接大改代码**，除非我下一条说「实现」。
