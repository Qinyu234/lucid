# 编号多需求实现

请按下列条目**逐条实现并逐条汇报**（完成一条再进入下一条；单条内保持最小 diff）：

1. {{需求 1}}  
2. {{需求 2}}  
3. {{需求 3}}  

## 项目约束（默认遵守）
- `src/`：`tools/verify_src_contract.py`；一文件一函数  
- 生成物：`io/output` 下 init/leaf/shared import 与 topology 模板  
- node：`semantic`、`function_name`、`io.in`/`io.out`、`role`、`topology`  
- 配置在 `config/`，产出在 `io/output/`，不要混放  

## 输出
每条：做了什么 → 涉及文件 → 如何验证（命令或检查点）
